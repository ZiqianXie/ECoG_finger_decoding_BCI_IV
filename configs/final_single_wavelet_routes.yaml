# One complete six-refit set is specified for each subject/finger pair.
# Every set contains six seeds of the same architecture and configuration.
default:
  ensemble_root: outputs/single_wavelet_1000hz_tap5over2_six_seed_refit_v1
  selection_root: outputs/single_wavelet_1000hz_tap5over2_nested_v2
  seeds: [0, 1, 2, 3, 4, 5]
  model_rate_hz: 1000
  tap_resample_up: 5
  tap_resample_down: 2
  csp_mode: movement_1
  recurrent_cell: standard
  little_event_decontamination: false

subjects:
  1:
    thumb:
      ensemble_root: outputs/full_development_single_wavelet_1000hz_s1_spatial_grid_ensemble_v1
      selection_root: outputs/single_wavelet_1000hz_paper_s1_spatial_grid_v1
      csp_mode: movement_2
      recurrent_cell: paper_equations
      development_oof_pcc: 0.5811437805548132
    index:
      ensemble_root: outputs/single_wavelet_1000hz_tap5over2_paper_equations_s1_six_seed_v1
      selection_root: outputs/single_wavelet_1000hz_tap5over2_paper_equations_s1_v1
      csp_mode: movement_1
      recurrent_cell: paper_equations
      development_oof_pcc: 0.7224760699841841
    middle:
      ensemble_root: outputs/final_single_wavelet_lstm_lr_selected_six_seed_v1
      selection_root: outputs/final_single_wavelet_lstm_lr_sweep_v2/lr1e-4
      csp_mode: tails_2x2
      recurrent_cell: standard
      selected_schedule: frozen_0
      development_oof_pcc: 0.248297245248044
    little:
      ensemble_root: outputs/full_development_single_wavelet_1000hz_s1_little_tails4x4_ensemble_v1
      selection_root: outputs/single_wavelet_1000hz_paper_s1_spatial_grid_v1
      csp_mode: tails_4x4
      recurrent_cell: paper_equations
      little_event_decontamination: true
      development_oof_pcc: 0.4681942620814756
  3:
    index:
      ensemble_root: outputs/final_single_wavelet_lstm_lr_selected_six_seed_v1
      selection_root: outputs/final_single_wavelet_lstm_lr_sweep_v2/lr1e-4
      csp_mode: movement_1
      recurrent_cell: standard
      selected_schedule: frozen_0
      development_oof_pcc: 0.5791861773164538
    little:
      ensemble_root: outputs/residual_recurrent_single_wavelet_s3_little_six_seed_v1
      selection_root: outputs/residual_recurrent_single_wavelet_oof_v1/residual_lstm/lr1e-4
      csp_mode: movement_1
      recurrent_cell: residual_lstm
      selected_schedule: frozen_150
      development_oof_pcc: 0.6499821371497261
